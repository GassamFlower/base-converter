# AGENT_CONSTITUTION — Base Converter 项目宪法

> 适用于「进制转换工具站」项目的所有 AI 协作。只写必须遵守的硬规则，目的是让 AI 在本项目里不跑偏、不烧钱、不出事故。
> 更新原则：每踩一个坑 / 每定一个新规则，往这里加一条（Keep 简单）。

---

## 一、项目身份

- 项目：Base Converter（进制互转纯静态工具站）
- 定位：练手跑通"挖需求→建站→上线→收录"流程的小站，不追求复杂度。
- 技术基线：纯静态 HTML/CSS/原生 JS + Vercel，**无后端、无数据库**。

---

## 二、强制遵循 / 硬规则

1. **技术选最笨的**：HTML+CSS+原生 JS 就够，**禁止**引入 React/Vue/Next.js/Vite/后端框架/数据库/ORM。
2. **不完善只上线**：先做出能跑通的最小可用 MVP 上线拿反馈，**禁止**无限打磨加功能延期。
3. **一次只做一件事**：每个阶段只做立项/选型/架构/上线其中一件，做完验收再做下一件，**禁止**跨阶段抢跑写代码。
4. **领域逻辑单一入口**：所有进制换算**必须**走唯一的 `convert()`，**禁止**在每个页面各自实现一套换算。
5. **验收才算完**：每个阶段对照 `docs/` 的判定标准自检通过才算完成，**禁止**口头"应该没问题"就当完成。
6. **文档与代码同步**：改了规则/边界，先更新 `docs/` 对应文档再动代码；临时想法先写回文档，**禁止**直接改完不落档。
7. **SEO 优先**：每个核心关键词对应页面要有独立 TDK + canonical；**禁止**无 TDK 的多余页面污染收录。
8. **诚实边界**：如实报告哪步已做、哪步没做、为何，AI 明知会失败时要说出来，**禁止**编造完成或夸大验证。

---

## 三、禁止事项（≥5 条）

- ❌ 不写后端 / 数据库 / 登录 / 付费功能（本阶段不是产品主战场）
- ❌ 不加广告 / 订阅 / 弹窗（先跑通自然流量，不要污染早期的站）
- ❌ 不让 AI 自由发挥写多套换算 / 多套样式轮子（强制单入口 + CSS 变量）
- ❌ 不把项目介绍 / 部署细节 / 所有细则塞进本宪法（细节放架构文档，宪法只在维持不可违反项）
- ❌ 一上来就让 AI 写代码（必须以各种文件为前提，替代讨论对齐定位）
- ❌ 过度流程化：纯静态站不强行按后端的"数据库/接口/权限/安全表"走（已适配）

## 四、框架复用 / 封装边界

- **必须用**：原生 HTML/CSS/JS、`BigInt`、`encodeURIComponent`、Vercel 静态托管。
- **可封装**：简单 CSS 变量 / 复制 helper / 输入格式化 helper。
- **禁止** 自造框架 / 路由库 / 虚拟 DOM。

## 五、验收标准（执行判定可用）

- [ ] 首页能打开，无报错，转换正确（BIN/DEC/HEX/OCT 示例 + 负数/大数边界）
- [ ] 所有换算走同一个 `convert()`，无重复实现
- [ ] 每个核心词页有独立 title/description/canonical
- [ ] sitemap.xml / robots.txt 存在并指向正确
- [ ] 已提交 git（每阶段一次，chore/feat 前缀规范）
- [ ] 已上线 Vercel 并可在浏览器访问

## 六、成本与上下文约束

- 忽略目录：`node_modules/` / `dist/` / `build/` / `logs/` / `.vscode/` / `.idea/`（本项目基本无，但如果出现不要碰）
- 模型分层：轻量模型做测试/检查、主力模型做核心生成、高端模型做审查（可协议时用）
- 会话熔断：单会话 ≤ 60 条，超长先 /compact 或开新会话来跑，避免 token 膨胀和心力消耗

---

*初始化版本，随项目推进持续增量更新（每踩坑/每定规则即加一条）。*