# AGENTS.md — IDE 入口

> 本文件只做"引用"，不复制宪法内容（保持单一真源，多处不漂移）。

本项目的一切 AI 协作行为，**必须遵守**根目录下 **`AGENT_CONSTITUTION.md`**。

---

## 进本项目第一步（任何 AI / 任何会话）

1. 先读 `AGENT_CONSTITUTION.md`（宪法）——里面是整个项目的硬规则。
2. 再读 `docs/00-推进路线图` 或对应阶段文档（立项/选型/架构等），确认现在在哪个阶段、该做什么不该做什么。
3. 按宪法"强制遵循"执行：技术最简、不完善先上线、单一 convert()、验证才算完、文档先于代码、SEO 优先、诚实边界。

## 宪法摘要（快速提醒，完整去宪法里看）

- 纯静态无后端无库，真选最简
- 每次只改 / 只做一个阶段，验收后再前进
- 换算只走 `convert()`，不各自造
- SEO：每核心词独立 TDK + canonical + sitemap
- git 每阶段一次，commit 前缀规范（chore/feat/fix/docs）
- 上下文：忽略 node_modules/dist/logs；会话该分段就分段

> 宪法才是真源。本文件如与宪法冲突，以宪法为准。